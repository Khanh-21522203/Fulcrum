[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "rate-limit-sidecar"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "grpcio>=1.60",
    "grpcio-tools>=1.60",
]

[tool.hatch.build.targets.wheel]
packages = ["rate_limit_sidecar"]
