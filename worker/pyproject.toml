[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "worker"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "psycopg[binary]>=3.2",
    "fulcrum-shared",
]

[tool.hatch.build.targets.wheel]
packages = ["worker"]
