[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "fulcrum-provider-local"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fulcrum-shared",
    "minio>=7.2",
]

[tool.hatch.build.targets.wheel]
packages = ["fulcrum_provider_local"]
