syntax = "proto3";

package envoy.config.core.v3;

// Minimal HeaderValue definitions needed by the Envoy sidecar APIs.
message HeaderValue {
  string key = 1;
  string value = 2;
}

message HeaderValueOption {
  HeaderValue header = 1;
}
